# ===============================================================
# i18n-service — PROD profile overrides
# Activated by: SPRING_PROFILES_ACTIVE=prod
# ===============================================================

# MongoDB — production cluster (MUST be supplied via environment variable or CI/CD secret)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://prod-mongo-host:27017/i18n-service}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:i18n-service}
