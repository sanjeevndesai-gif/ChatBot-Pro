import { ExpiryDateMask } from './expiry-date-mask';

describe('ExpiryDateMask', () => {
  it('should create an instance', () => {
    const directive = new ExpiryDateMask();
    expect(directive).toBeTruthy();
  });
});
