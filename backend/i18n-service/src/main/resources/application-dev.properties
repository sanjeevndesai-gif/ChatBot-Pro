# ===============================================================
# i18n-service — DEV profile overrides
# Activated by: SPRING_PROFILES_ACTIVE=dev
# ===============================================================

# MongoDB — dev cluster
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://dev-mongo-host:27017/i18n-service}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:i18n-service}
