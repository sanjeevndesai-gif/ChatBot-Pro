# ===============================================================
# chat — DEV profile overrides
# Activated by: SPRING_PROFILES_ACTIVE=dev
# ===============================================================

# MongoDB — dev cluster
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://sanjeevndesai_db_user:Sanpug%402601@chatbot.wqqueab.mongodb.net/auth-service?retryWrites=true&w=majority}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:chatdb}

# Redis — dev instance
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:dev-redis-host}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:}
