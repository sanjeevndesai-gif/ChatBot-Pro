# ===============================================================
# book_appointment — PROD profile overrides
# Activated by: SPRING_PROFILES_ACTIVE=prod
# ===============================================================

# MongoDB — production cluster (MUST be supplied via environment variable or CI/CD secret)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://prod-mongo-host:27017/book_appointment}

# ATS Config
ats.mongodbUri=${ATS_MONGODB_URI:mongodb://prod-mongo-host:27017}
