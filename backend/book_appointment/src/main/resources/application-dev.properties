# ===============================================================
# book_appointment — DEV profile overrides
# Activated by: SPRING_PROFILES_ACTIVE=dev
# ===============================================================

# MongoDB — dev cluster
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://dev-mongo-host:27017/book_appointment}

# ATS Config
ats.mongodbUri=${ATS_MONGODB_URI:mongodb://dev-mongo-host:27017}
