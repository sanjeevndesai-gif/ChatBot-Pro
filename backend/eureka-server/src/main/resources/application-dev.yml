spring:
  profiles: dev

server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: true
  server:
    eviction-interval-timer-in-ms: 60000

jwt:
  secret: ${JWT_SECRET:dev-secret}

management:
  endpoints:
    web:
      exposure:
        include: health,info
